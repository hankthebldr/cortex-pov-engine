scenario_id: SIM-CLOUD-004
name: "Cloud App — Cross-Provider OAuth Grant Rotation (Okta → MS → Google)"
version: "1.0"
status: active

plane: CLOUD_APP
detection_types:
  - XQL

uc_ref: UCS-ITDR-02
tc_ref: TC-ITDR-03
tc_refs:
  - TC-ITDR-03
  - TC-DLP-11
pov_scenario_id: POV-SC-001
uc_name: "Identity Threat Detection & Response"
tc_name: "Validate CIE syncs and correlates AD, Azure AD, and cloud IdPs"

mitre_tactic: "TA0005"
mitre_tactic_name: "Defense Evasion"
mitre_technique: "T1090"
mitre_technique_name: "Proxy"
additional_techniques:
  - technique: "T1550.001"
    name: "Use Alternate Authentication Material: Application Access Token"
  - technique: "T1528"
    name: "Steal Application Access Token"

threat_report: "Unit42 - Multi-IdP OAuth Phishing Campaigns"
threat_report_url: "https://unit42.paloaltonetworks.com/"

execution_identity:
  default: container-runtime
  options:
    - container-runtime
    - root

push_supported: true
pull_supported: true

infra_modules_needed:
  - base

# ── v2.0 KPI / Methodology Metadata ─────────────────────────────────────────
# Rolled out per the S-07 per-plane convention
# (docs/uc_tc_mapping/scenario-methodology-defaults.md). Defaults are not
# fabricated KPIs — they map this plane to the methodology family its
# detection shape already belongs to in the v2.0 master methodology.
validation_methodology: "OAuth-Grant Signal Injection & Detection Accuracy"
methodology_family: "F1"
primary_kpi: "Detection Accuracy"
threshold:
  kpi: "Detection Accuracy"
  op: "≥"
  value: 90
  unit: "%"
success_criteria: |
  The outbound OAuth-authorize requests with planted risky scopes produce
  the expected Cortex Cloud App Security detection. Detection Accuracy is
  the share of injected grants that raise the expected alert (target ≥ 90%).
moat_tier: "LEAD"

# ── Causality contract ──────────────────────────────────────────────────────
# The oauth_grant_emulator (python3, container-runtime) is the Causality Group
# Owner — the single identity that hops Okta → Microsoft → Google — NOT the
# synthetic beacon. Each subsequent IdP grant is the SAME identity soliciting at
# the NEXT provider with no direct process fork (a shared_entity pivot on
# `user` / shared run-ID prefix / source_ip): this provider-hopping is precisely
# the policy-evasion behaviour CR-CLOUDAPP-0004 stitches into one incident.
cgo_anchor:
  image_name: "python3"
  primary_username: "container-runtime"

external_tools:
  - name: cortexsim-eal-cli
    source: "https://github.com/hankthebldr/cortex-pov-engine"
    type: script
    install_inline: false

steps:
  - id: step-01
    name: "Provider 1 — Okta risky drive grant"
    command: |
      cat > /tmp/cloud-004-okta.yml <<'YML'
      campaign_id: CMP-CLOUDAPP-004
      name: "Cloud App — rotation step 1 Okta"
      authorized_by: "domain-consultant@paloaltonetworks.com"
      simulation_authorized: true
      target_allowlist:
        - cortexsim-canary.okta.com
      dry_run: false
      tags: [cloud-app, cross-provider, rotation]
      steps:
        - step_id: step-01
          plugin: oauth_grant_emulator
          params:
            provider: okta
            scope_preset: risky_drive
            iterations: 1
      YML
      python3 -m scripts.eal_simulator.cli run /tmp/cloud-004-okta.yml --live
    identity: container-runtime
    mitre_technique: "T1550.001"
    platforms: ["linux"]
    expected_detections:
      - plane: CLOUD_APP
        type: XQL
        description: "Cloud App — first risky grant to Okta, identity marked active in IdP matrix"
        ttp_ref: "TTP-2026-0030"
        detection_id: xql-cloud-004-okta-first-risky-grant-identity-active-in-idp-matrix

  - id: step-02
    name: "Provider 2 — Microsoft admin-consent grant within 60s"
    command: |
      sleep 30
      cat > /tmp/cloud-004-ms.yml <<'YML'
      campaign_id: CMP-CLOUDAPP-004
      name: "Cloud App — rotation step 2 Microsoft"
      authorized_by: "domain-consultant@paloaltonetworks.com"
      simulation_authorized: true
      target_allowlist:
        - login.microsoftonline.com
      dry_run: false
      steps:
        - step_id: step-01
          plugin: oauth_grant_emulator
          params:
            provider: microsoft
            scope_preset: admin_consent
            iterations: 1
      YML
      python3 -m scripts.eal_simulator.cli run /tmp/cloud-004-ms.yml --live
    identity: container-runtime
    mitre_technique: "T1090"
    platforms: ["linux"]
    causality:
      parent_step: step-01
      pivot: shared_entity
    expected_detections:
      - plane: CLOUD_APP
        type: XQL
        description: "Cloud App — same identity solicits admin-consent grant at second IdP within rotation window"
        ttp_ref: "TTP-2026-0030"
        detection_id: xql-cloud-004-microsoft-admin-consent-grant-second-idp-in-rotation-window

  - id: step-03
    name: "Provider 3 — Google risky drive grant completes triangle"
    command: |
      sleep 30
      cat > /tmp/cloud-004-google.yml <<'YML'
      campaign_id: CMP-CLOUDAPP-004
      name: "Cloud App — rotation step 3 Google"
      authorized_by: "domain-consultant@paloaltonetworks.com"
      simulation_authorized: true
      target_allowlist:
        - accounts.google.com
      dry_run: false
      steps:
        - step_id: step-01
          plugin: oauth_grant_emulator
          params:
            provider: google
            scope_preset: risky_drive
            iterations: 1
      YML
      python3 -m scripts.eal_simulator.cli run /tmp/cloud-004-google.yml --live
    identity: container-runtime
    mitre_technique: "T1090"
    platforms: ["linux"]
    causality:
      parent_step: step-02
      pivot: shared_entity
    expected_detections:
      - plane: CLOUD_APP
        type: XQL
        description: "Cloud App — three IdPs within 5 min from same identity — provider-hopping policy evasion alert"
        ttp_ref: "TTP-2026-0030"
        detection_id: xql-cloud-004-three-idps-same-identity-provider-hopping-policy-evasion
      - plane: ANALYTICS
        type: XQL
        description: "XSIAM stitching — single identity correlates the three IdP sessions into one incident via X-Simulation-Run-ID prefix"
        ttp_ref: "TTP-2026-0030"
        detection_id: xql-cloud-004-xsiam-cross-provider-rotation-single-incident-stitch
      - plane: NDR
        type: XQL
        description: "NGFW EAL — three /authorize App-ID hits across three FQDNs sharing run marker"
        ttp_ref: "TTP-2026-0030"
        detection_id: xql-cloud-004-ngfw-three-authorize-hits-across-three-fqdns-shared-run-marker

cleanup:
  commands:
    - "rm -f /tmp/cloud-004-*.yml"

author: "Henry Reed"
created: "2026-05-15"
last_updated: "2026-05-15"
tags:
  - cloud-app
  - casb
  - cross-provider
  - stitching
  - policy-evasion
  - eal-plugin
