title: "AI Access \u2014 AWS Access Key Leaked to Anthropic API \u2014 AIACC-002 Pre-flight Secret Payload Contains AKIA Canary"
id: ttp-2026-0008-bioc-01
status: experimental
description: |
  Pre-flight: secret payload contains AKIA-prefixed canary. Confirms the secret payload renders the AKIA0000CORTEXSIMCANARY marker on the worker before any egress; matches the python self-check process invocation.
  Canonical Cortex XQL body follows (preserve verbatim for XSIAM):
  ----
    preset = xdr_data
    | filter event_type = ENUM.PROCESS
    | filter event_sub_type = ENUM.PROCESS_START
    | filter actor_process_image_name in ("python", "python3")
    | filter action_process_image_command_line contains "_render_payload"
    | filter action_process_image_command_line contains "AKIA0000CORTEXSIMCANARY"
  ----
references:
  - https://attack.mitre.org/techniques/T1552/
author: CortexSim detection_scanner
tags:
    - attack.t1552.001
logsource:
  product: cross-platform
  category: process_creation
detection:
  selection:
    placeholder: replace_with_target_dialect_translation_of_xql
  condition: selection
falsepositives:
  - Legitimate administrative tooling that mirrors the same telemetry shape.
level: medium
