title: "Periodic HTTP C2 Beaconing \u2014 NGFW EAL Network Detection (T1071.001) \u2014 NGFW EAL \u2014 outbound HTTP carrying X-Simulation-Run-ID header (operator filter)"
id: ttp-2026-0050-bioc-01
status: experimental
description: |
  NGFW EAL — outbound HTTP carrying X-Simulation-Run-ID header (operator filter)
  Canonical Cortex XQL body follows (preserve verbatim for XSIAM):
  ----
    dataset = panw_ngfw_traffic_raw
    | filter app = "web-browsing" or app = "http"
    | filter http_req_headers contains "X-Simulation-Run-ID"
    | filter action = "allow"
    | fields _time, src_ip, dst_ip, dst_host, app, uri, http_user_agent, http_req_headers
  ----
references:
  - https://attack.mitre.org/techniques/T1071/
author: CortexSim detection_scanner
tags:
    - attack.t1071.001
logsource:
  product: linux
  category: process_creation
detection:
  selection:
    placeholder: replace_with_target_dialect_translation_of_xql
  condition: selection
falsepositives:
  - Legitimate administrative tooling that mirrors the same telemetry shape.
level: high
