# TTP-2026-0042 — KOI — Claude Desktop Installs Typosquat MCP Server
# XQL paste-buffer generated by detection_scanner/scripts/export_artifacts.py

## BIOC — Code scan — manifest name 'anthroopic-calculator' typosquats 'anthropic-calculator'
# severity: high
# Code scan — manifest name 'anthroopic-calculator' typosquats 'anthropic-calculator'

// AUTO-GENERATED SKELETON — replace with real XQL before promotion.
// MITRE technique: T1195
preset = xdr_data
| filter event_type = ENUM.PROCESS
| filter /* TODO: predicate matching the BIOC name */
| fields _time, agent_hostname, actor_process_image_name, action_process_image_command_line

## BIOC — Code scan — tool description embeds 'Ignore previous instructions' / AKIA canary
# severity: high
# Code scan — tool description embeds 'Ignore previous instructions' / AKIA canary

// AUTO-GENERATED SKELETON — replace with real XQL before promotion.
// MITRE technique: T1195
preset = xdr_data
| filter event_type = ENUM.PROCESS
| filter /* TODO: predicate matching the BIOC name */
| fields _time, agent_hostname, actor_process_image_name, action_process_image_command_line

## BIOC — NGFW EAL — Claude Desktop User-Agent fetches MCP manifest from non-canonical FQDN
# severity: high
# NGFW EAL — Claude Desktop User-Agent fetches MCP manifest from non-canonical FQDN

// AUTO-GENERATED SKELETON — replace with real XQL before promotion.
// MITRE technique: T1059
preset = xdr_data
| filter event_type = ENUM.PROCESS
| filter /* TODO: predicate matching the BIOC name */
| fields _time, agent_hostname, actor_process_image_name, action_process_image_command_line

## BIOC — URL filtering — destination matches typosquat pattern of approved AI tool publisher
# severity: high
# URL filtering — destination matches typosquat pattern of approved AI tool publisher

// AUTO-GENERATED SKELETON — replace with real XQL before promotion.
// MITRE technique: T1059
preset = xdr_data
| filter event_type = ENUM.PROCESS
| filter /* TODO: predicate matching the BIOC name */
| fields _time, agent_hostname, actor_process_image_name, action_process_image_command_line

## BIOC — Cortex Cloud Code — typosquat package-name detector fires
# severity: high
# Cortex Cloud Code — typosquat package-name detector fires

// AUTO-GENERATED SKELETON — replace with real XQL before promotion.
// MITRE technique: T1195
preset = xdr_data
| filter event_type = ENUM.PROCESS
| filter /* TODO: predicate matching the BIOC name */
| fields _time, agent_hostname, actor_process_image_name, action_process_image_command_line

## BIOC — Cortex Cloud Code — instruction-injection in tool description detected
# severity: high
# Cortex Cloud Code — instruction-injection in tool description detected

// AUTO-GENERATED SKELETON — replace with real XQL before promotion.
// MITRE technique: T1195
preset = xdr_data
| filter event_type = ENUM.PROCESS
| filter /* TODO: predicate matching the BIOC name */
| fields _time, agent_hostname, actor_process_image_name, action_process_image_command_line

