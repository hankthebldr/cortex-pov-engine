# TTP-2026-0007 — AI Access — Source Code Paste to Public ChatGPT
# XQL paste-buffer generated by detection_scanner/scripts/export_artifacts.py

## BIOC — Pre-flight: plugin metadata visible to operator
# severity: high
# Pre-flight: plugin metadata visible to operator

// AUTO-GENERATED SKELETON — replace with real XQL before promotion.
// MITRE technique: T1567
dataset = http_proxy
| filter dst_host IN ("api.openai.com", "api.anthropic.com", "generativelanguage.googleapis.com")
| filter /* TODO: predicate matching the BIOC name */
| fields _time, agent_hostname, actor_process_image_name, action_process_image_command_line

## BIOC — AI Access — outbound POST to api.openai.com containing source-code shape (CORTEXSIM-CANARY marker)
# severity: high
# AI Access — outbound POST to api.openai.com containing source-code shape (CORTEXSIM-CANARY marker)

// AUTO-GENERATED SKELETON — replace with real XQL before promotion.
// MITRE technique: T1567
dataset = http_proxy
| filter dst_host IN ("api.openai.com", "api.anthropic.com", "generativelanguage.googleapis.com")
| filter /* TODO: predicate matching the BIOC name */
| fields _time, agent_hostname, actor_process_image_name, action_process_image_command_line

## BIOC — AI Access — DLP regex match on source-code structure in prompt body
# severity: high
# AI Access — DLP regex match on source-code structure in prompt body

// AUTO-GENERATED SKELETON — replace with real XQL before promotion.
// MITRE technique: T1567
dataset = http_proxy
| filter dst_host IN ("api.openai.com", "api.anthropic.com", "generativelanguage.googleapis.com")
| filter /* TODO: predicate matching the BIOC name */
| fields _time, agent_hostname, actor_process_image_name, action_process_image_command_line

## BIOC — NGFW EAL — generative-AI App-ID match on outbound HTTPS to api.openai.com
# severity: high
# NGFW EAL — generative-AI App-ID match on outbound HTTPS to api.openai.com

// AUTO-GENERATED SKELETON — replace with real XQL before promotion.
// MITRE technique: T1567
dataset = http_proxy
| filter dst_host IN ("api.openai.com", "api.anthropic.com", "generativelanguage.googleapis.com")
| filter /* TODO: predicate matching the BIOC name */
| fields _time, agent_hostname, actor_process_image_name, action_process_image_command_line

## BIOC — AI Access — repeated POST pattern (3 in <60s) flagged as policy violation
# severity: high
# AI Access — repeated POST pattern (3 in <60s) flagged as policy violation

// AUTO-GENERATED SKELETON — replace with real XQL before promotion.
// MITRE technique: T1567
dataset = http_proxy
| filter dst_host IN ("api.openai.com", "api.anthropic.com", "generativelanguage.googleapis.com")
| filter /* TODO: predicate matching the BIOC name */
| fields _time, agent_hostname, actor_process_image_name, action_process_image_command_line

