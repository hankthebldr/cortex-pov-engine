adapter_id: TOOL-RECON-NG
name: "recon-ng"
version: "5.1.2"

tier: 4
category: network-scan

upstream:
  repo: https://github.com/lanmaster53/recon-ng
  license: "GPL-3.0"
  attribution: "Tim Tomes"

cortex_signal:
  planes:
    - NDR
  expected_techniques:
    - T1595
    - T1592

safety_class: safe

install:
  runtime_install_command: "command -v recon-ng >/dev/null 2>&1 || pip install recon-ng"
  binary: "recon-ng"
  # ── Payload-shelf exemption (TA-13) ──────────────────────────────────────
  artifact_exempt:
    reason_code: LANGUAGE_PACKAGE_MANAGER
    reason: >-
      Recon-ng installs from the Python (pip) package index along with its
      dependency tree. The shelf stages one file, and there is no single file
      that runs. Staging it would mean hosting a package mirror, so this tool
      needs internet access on the target host.

invoke:
  target_platform: linux
  run_template: "{binary} -m {module} -x run"
  default_args:
    module: "recon/domains-hosts/hackertarget"
  identity_required: root

ttp_refs: []
equivalents: []

author: Henry Reed
created: "2026-06-02"
last_updated: "2026-06-02"
tags:
  - phase-c
  - network-scan
